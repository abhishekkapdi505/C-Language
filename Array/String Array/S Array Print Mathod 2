#include <stdio.h>
#include <conio.h>
#include <string.h>
#include <math.h>

int main () {

 char name[15];

 int i,len;

printf("Enter a Name::");
scanf("%s", name);

len = strlen(name);

for(i = 0; i<len; i++) {
 printf("%c", name[i]);

}

 getch();
 return 0;

}
