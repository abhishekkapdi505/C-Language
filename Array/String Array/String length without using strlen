#include <stdio.h>
#include <conio.h>
#include <string.h>

int main () {

 char name[15];

 int i=0,len;

printf("Enter a Name::");
scanf("%s", name);

printf("Name is::%s", name);

while(name[i] != '\0') {
 
 len = len +1;

i++;

}

printf("\nlength::%d", len);

 getch();
 return 0;

}
