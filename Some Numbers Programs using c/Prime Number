#include<stdio.h>
#include<conio.h>

int main (){
 

int i,n,count=0;

printf("Enter a Number::");
scanf("%d", &n);

for(i = 2; i<n; i++) {

 if(n % i == 0) { 
  
 count++;
 break;
} 
}
 
 if (count != 0) { 
 printf("Not Prime");

} else { 
 printf("Prime");
} 



getch();
return 0;

}
